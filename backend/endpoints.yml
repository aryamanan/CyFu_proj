# This file contains the different endpoints your Rasa server will connect to.

# Server where the models are pulled from.
# https://rasa.com/docs/rasa/model-storage#fetching-models-from-a-server

#models:
#  url: http://my-server.com/models/default@latest
#  wait_time_between_pulls:  10   # [optional](default: 100)

# Server endpoint triggered by Rasa for custom actions.
# https://rasa.com/docs/rasa/custom-actions#action-server-setup

action_endpoint:
 url: "http://localhost:5055/webhook"

# Tracker store which is used to store conversation sessions.
# https://rasa.com/docs/rasa/tracker-stores
#tracker_store:
#    type: redis
#    url: <host of the redis instance, e.g. localhost>
#    port: <port of your redis instance, e.g. 6379>
#    db: <number of your database, e.g. 0>
#    password: <password used for authentication>
#    use_ssl: <whether or not the communication is encrypted, default false>

#tracker_store:
#    type: mongod
#    url: <url to your mongo instance, e.g. mongodb://localhost:27017>
#    db: <name of your database, e.g. rasa>
#    username: <username for authentication>
#    password: <password for authentication>

# Event broker which all conversation events should be streamed to.
# https://rasa.com/docs/rasa/event-brokers

#event_broker:
#  url: localhost
#  username: username
#  password: password
#  queue: queue
